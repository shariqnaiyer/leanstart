namespace: lean-devnet

genesis:
  configMapName: genesis-config
  pvcName: genesis-data
  storageClass: ""
  storageSize: 5Gi

# Populated by `lean-devnet generate`
clients: []

initScripts:
  resolverImage: busybox:1.36

bootnodeCount: 5

prometheus:
  enabled: true
