{{/*
  Per-pod secrets are generated externally by `lean-devnet generate`
  and applied via `kubectl apply -f output/secrets/` before helm install.

  This template is a placeholder. For in-chart secret management,
  the secrets would be templated here from values. Currently they
  are managed as separate manifests to avoid bloating the Helm release
  with potentially thousands of Secret objects.
*/}}
